; kernel_runner.art — interpret a program file with the kernel
; (interpret mode) in its own process, so its stdout is the
; program's stdout.
import ./"kernel.art"
import ./"modules.art"

go: function [][
    if 0 < size args\values [
        src: resolveImportsFromFile args\values\0
        interpret src
    ]
]
go
